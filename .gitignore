node_modules/
.DS_Store
dist/
dist-ssr/
*.local
.env
.env.*
!.env.example
.vercel

# Jeton d acces MCP — ne jamais commiter
.agents/mcp_config.json

# Export brut de Claude Design (23 Mo) — on ne versionne que le .dc.html
docs/design/*.zip

# Archives de backup locales
backups/
